{
  "name": "railgaadi",
  "version": "1.0.0",
  "description": "RailGaadi - Real-Time Indian Railway Intelligence and Journey Companion",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"green,cyan\" \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "build": "npm run build:backend && npm run build:frontend",
    "pm2:start": "npm run build:backend && pm2 start ecosystem.config.cjs",
    "pm2:stop": "pm2 stop all",
    "pm2:restart": "npm run build:backend && pm2 restart all",
    "pm2:status": "pm2 list",
    "pm2:logs": "pm2 logs",
    "pm2:save": "pm2 save",
    "pm2:delete": "pm2 delete all"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
